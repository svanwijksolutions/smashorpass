# Reference material (not used on the site)

Source files supplied by the client, kept here so the menu text and the logo can be
checked against the originals.

- `menu-original.jpg` — photo of the printed SMASH BURGER MENU. The website menu is
  built as real HTML text in six languages, this image is only the source of truth.
- The logo badge in `../images/logo.svg` and `../images/logo-compact.svg` is a hand
  drawn SVG recreation of `../images/logo-reference.jpg`.
